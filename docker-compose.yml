version: "3.9"

services:
  redis:
    image: redis:7
    container_name: leaderboard-redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    command: >
      redis-server
      --appendonly yes
      --appendfsync everysec
volumes:
  redis-data: